/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

import { describe, it, expect, TestType, Size, Level, beforeAll } from "../../../hypium/index";
import hilog from '@ohos.hilog'
import { BusinessError } from '@ohos.base';
import common from '@ohos.app.ability.common';
import UIAbility from '@ohos.app.ability.UIAbility';
import text from "@ohos.graphics.text"
import type common2D from '@ohos.graphics.common2D';
import Utils from './Util.test';

let domain: number = 0x0000; //日志标识,0x0000作为测试框架的业务标识
let tag: string = 'testTag'; //日志标识字符串,作为tag标识当前runner类下的测试行为

export default function GraphicTextParagraphBuilder() {
  describe("GraphicTextParagraphBuilder", (): void => {
    beforeAll(() => {
      hilog.info(domain, tag, '%{public}s', 'beforeAll start');
    })

    /**
     * @tc.number    : testCreateParagraphBuilderStatic
     * @tc.name      : testCreateParagraphBuilderStatic
     * @tc.desc      : To test for create a paragraphBuilder.
     * @tc.size      : MediumTest
     * @tc.type      : Function
     * @tc.level     : LEVEL1
     */
    it('testCreateParagraphBuilderStatic', TestType.FUNCTION | Size.MEDIUMTEST | Level.LEVEL1,
      async (done: () => void): Promise<void> => {
        try {
          let fontCollection = text.FontCollection.getGlobalInstance()
          expect(fontCollection !== undefined).assertTrue()
          fontCollection.loadFontSync('myFamilyName', 'file:///system/fonts/HarmonyOS_Sans.ttf')
          let myFontFamily: Array<string> = ["myFamilyName"]
          let myColor: common2D.Color = { alpha: 255, red: 255, green: 0, blue: 0 };
          let myTextStyle: text.TextStyle = {
            color: myColor,
            fontSize: 100,
            fontFamilies: myFontFamily
          };
          let myParagraphStyle: text.ParagraphStyle = {
            textStyle: myTextStyle,
            align: text.TextAlign.JUSTIFY,
            wordBreak: text.WordBreak.NORMAL
          };
          let paragraphBuilder = new text.ParagraphBuilder(myParagraphStyle, fontCollection)
          expect(paragraphBuilder !== undefined).assertTrue()
          paragraphBuilder.pushStyle(myTextStyle);
          paragraphBuilder.addText("Hello World!");
          let paragraph = paragraphBuilder.build();
          expect(paragraph !== undefined).assertTrue()
          hilog.info(domain, tag, 'Succeed in fontCollection.loadFontSync.');
          expect(true).assertTrue();
          done()
        } catch (err) {
          hilog.error(domain, tag, `Failed in fontCollection.loadFontSync. Cause:${JSON.stringify(err)}`);
          expect().assertFail();
          done()
        }
      })
  })
}